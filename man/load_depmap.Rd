% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{load_depmap}
\alias{load_depmap}
\title{Download filtered DepMap data}
\usage{
load_depmap(dest = ".", read = FALSE, timeout_seconds = 600, retries = 1)
}
\arguments{
\item{dest}{Directory to save the downloaded file. Default = ".".}

\item{read}{Whether to read the data and assign to global environment as "DepMap".
Default = FALSE.}

\item{timeout_seconds}{Numeric, timeout for each download attempt in seconds. Default = 600.}

\item{retries}{Integer, number of retries for each mirror. Default = 1.}
}
\value{
Invisibly returns the DepMap object if read = TRUE, otherwise NULL.
}
\description{
Downloads the filtered DepMap RDS file for training models from GitHub Release.
The file contains bulk expression, scRNA expression, drug response annotations,
and cell line metadata required for PERCEPTION model training.
Supports automatic mirror fallback for users in different regions.
}
\examples{
\dontrun{
# Download only (no loading)
load_depmap()

# Download, read, and assign to global environment
load_depmap(read = TRUE)
# Then access DepMap$expression_rnorm, DepMap$scRNA_complete, etc.
}
}
