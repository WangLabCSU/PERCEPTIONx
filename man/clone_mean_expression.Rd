% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{clone_mean_expression}
\alias{clone_mean_expression}
\title{Compute clone-level mean expression from single-cell data}
\usage{
clone_mean_expression(expression_matrix, cell_clone_map, patient_ids = NULL)
}
\arguments{
\item{expression_matrix}{Matrix or data frame. Gene expression matrix
with genes as rows and cells as columns.}

\item{cell_clone_map}{Data frame with columns \code{cell_id} and \code{clone_id},
mapping each cell to its clone. Typically from Seurat clustering.}

\item{patient_ids}{Character vector. Patient ID for each cell, same length as
columns of expression_matrix. If NULL, all cells are assumed from one patient.}
}
\value{
A named list of matrices, one per patient. Each matrix has genes as rows
and clone IDs as columns, with mean expression values.
}
\description{
For each patient, groups cells by clone_id and computes the mean
expression of each gene within each clone. This reduces single-cell
resolution to subclone resolution, matching the original PERCEPTION pipeline.
}
