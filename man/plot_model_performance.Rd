% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_model_performance}
\alias{plot_model_performance}
\title{Plot model performance across datasets}
\usage{
plot_model_performance(
  performance_list,
  threshold_range = NULL,
  base_size = 20,
  highlight_threshold = 0.3,
  tooltip = TRUE
)
}
\arguments{
\item{performance_list}{Named list of model performance objects (from train_perception_models).}

\item{threshold_range}{Numeric vector. Correlation thresholds to evaluate.
Default = NULL (auto: from 0.1 up to at least 0.6, extended to the
highest observed correlation so the curve always reaches 0).}

\item{base_size}{Numeric. Base font size. Default = 20.}

\item{highlight_threshold}{Numeric. Threshold to highlight with vertical line.
Default = 0.3.}

\item{tooltip}{Logical. If TRUE (default) and \pkg{ggiraph} is installed,
points get hover tooltips (dataset, threshold, drug count).}
}
\value{
A ggplot object.
}
\description{
Visualizes the number of drugs achieving different correlation thresholds
across bulk, pseudo-bulk, and single-cell datasets.
}
\examples{
\dontrun{
  models <- train_perception_models(c("abemaciclib", "erlotinib"), ...)
  plot_model_performance(models)
}

}
