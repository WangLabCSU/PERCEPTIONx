% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{annotate_clones}
\alias{annotate_clones}
\title{PERCEPTION Patient Data Annotation Functions}
\usage{
annotate_clones(
  expression_matrix,
  min_cells = 3,
  min_features = 200,
  nfeatures = 2000,
  dims = 10,
  resolution = 0.8,
  seed = 42
)
}
\arguments{
\item{expression_matrix}{Matrix. Gene expression matrix with genes as rows
and cells as columns. Raw counts or normalized values are both accepted.}

\item{min_cells}{Integer. Minimum cells per feature. Default = 3.}

\item{min_features}{Integer. Minimum features per cell. Default = 200.}

\item{nfeatures}{Integer. Number of variable features. Default = 2000.}

\item{dims}{Integer. Number of PCA dimensions for clustering. Default = 10.}

\item{resolution}{Numeric. Clustering resolution. Default = 0.8.}

\item{seed}{Integer. Random seed for reproducibility. Default = 42.}
}
\value{
A data frame with columns: \code{cell_id} and \code{clone_id}.
}
\description{
Functions for annotating single-cell data with clone and patient information,
and preparing patient data for the prediction pipeline.
Annotate cells with clone IDs via Seurat clustering
}
\details{
Performs Seurat clustering on a single-cell expression matrix and returns
a mapping of each cell to its cluster (clone) ID. This matches the original
PERCEPTION pipeline where Seurat clusters define transcriptional subclones.
}
\examples{
\dontrun{
  cell_clone_map <- annotate_clones(patient_expression)
}

}
