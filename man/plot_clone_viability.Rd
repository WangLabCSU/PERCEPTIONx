% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_clone_viability}
\alias{plot_clone_viability}
\title{Plot clone-level viability (lollipop plot)}
\usage{
plot_clone_viability(
  clone_viability,
  viability_var = "comb_viability",
  weights_var = NULL,
  response_var = NULL,
  drug = NULL,
  base_size = 11,
  y_limits = c(-3, 1.2),
  viridis_scale = FALSE,
  tooltip = TRUE,
  tooltip_col = NULL
)
}
\arguments{
\item{clone_viability}{Data frame with columns: patient, clone_id, and the
viability column named by \code{viability_var}. Optionally also
\code{weights_var} (clone proportion) and \code{response_var}
(clinical response) columns.}

\item{viability_var}{Character. Column name for viability values. Default = "comb_viability".}

\item{weights_var}{Character. Optional column name for clone weights (point size).
Default = NULL.}

\item{response_var}{Character. Optional column for response annotation.
Default = NULL.}

\item{drug}{Character. Drug name, used as plot title. Default = NULL.}

\item{base_size}{Numeric. Base font size. Default = 11.}

\item{y_limits}{Numeric vector. Y-axis limits. Default = c(-3, 1.2).}

\item{viridis_scale}{Logical. If TRUE, uses a viridis sequential scale
(dark = low/sensitive, yellow = high/resistant). If FALSE (default),
uses the diverging red-blue scale (blue = low/sensitive, white =
neutral, red = high/resistant) with data-driven limits, so every
value keeps a real color — nothing clips to grey/NA outside a
fixed window.}

\item{tooltip}{Logical. If TRUE (default) and \pkg{ggiraph} is installed,
points get hover tooltips (clone + viability + proportion).}

\item{tooltip_col}{Character. Optional existing column used as the tooltip
text. Default = NULL (auto-builds a rich tooltip).}
}
\value{
A ggplot object.
}
\description{
Visualizes predicted drug sensitivity for each clone within patients.
Each clone is represented as a point with a stem (lollipop style).
Useful for identifying resistant clones within heterogeneous tumors.
}
\details{
Facet strategy is adaptive:
\itemize{
\item <= 12 patients: single-row grid, strips on the bottom (45 deg).
\item > 12 patients: \code{facet_wrap} grid, one compact panel per patient.
}
}
\examples{
\dontrun{
  clone_kill <- data.frame(
    patient = c("P1", "P1", "P2", "P2"),
    clone_id = c("c1", "c2", "c1", "c2"),
    comb_viability = c(-0.5, 0.8, -1.2, 0.3)
  )
  plot_clone_viability(clone_kill, viability_var = "comb_viability")
}

}
